name: face_detection
description: face_detection
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_mlkit_face_detection: ^0.8.0
  image_pickers: ^2.0.4+1
  image_picker: ^1.0.2
  camera: ^0.10.5+2
  path_provider: ^2.0.15
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
